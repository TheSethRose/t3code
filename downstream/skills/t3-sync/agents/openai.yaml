interface:
  display_name: "T3 Sync"
  short_description: "Sync exact upstream into downstream"
  default_prompt: "Use $t3-sync to merge the latest upstream T3 Code into downstream main and reconcile active overlays."
